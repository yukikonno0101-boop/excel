
{
  "name": "excel-concierge-serverless",
  "version": "1.0.0",
  "private": true,
  "description": "Excel formula generator with a serverless backend",
  "scripts": {
    "build": "echo 'No build step required for frontend. Vercel will handle the API.'",
    "dev": "echo 'Please use Vercel CLI for local development: vercel dev'"
  },
  "dependencies": {
    "@google/genai": "^1.13.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12"
  }
}
