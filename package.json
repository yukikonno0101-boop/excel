{
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No client-side build step required'"
  },
  "dependencies": {
    "@google/genai": "^1.13.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@vercel/node": "^3.0.0"
  }
}